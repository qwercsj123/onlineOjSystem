# 线上配置文件
server:
  port: 8201
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 生产环境关闭日志
# 接口文档的配置
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.csj.ojbackend.controller
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true

SandBox:
  type: remote